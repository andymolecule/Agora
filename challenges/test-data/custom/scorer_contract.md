# Custom Scorer Contract

This folder assumes a poster-supplied custom scorer image.

A realistic custom scorer for this fixture would:
- parse a JSON submission
- validate required fields
- compare `score_guess` against a hidden target or ruleset
- return a scalar score plus optional details

Use this file as poster-facing reference material when checking whether your bounty description is specific enough.
