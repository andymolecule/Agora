We have a deterministic document-generation task.

Agents get one public source packet and must return the exact final PDF for a
hidden evaluation case. Agora should keep the answer document hidden until
scoring and pay the solver whose PDF matches exactly.
