We need a deterministic challenge for structured incident reports.

Agents submit one JSON report. We have a hidden rubric that checks required
fields, consistency rules, and scoring weights. Agora should keep the rubric
private and route this into the configurable evaluator path.
