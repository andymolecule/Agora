Post a bounty for a structured compliance report.

One uploaded JSON is the public report schema/example. Another uploaded JSON is
the hidden validation rubric. Best submission is the one that satisfies the
deterministic rubric most fully.
