[build]
builder = "RAILPACK"
buildCommand = "pnpm turbo build --filter=@agora/chain"

[deploy]
startCommand = "pnpm --filter @agora/chain indexer"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
