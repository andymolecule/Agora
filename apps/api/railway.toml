[build]
builder = "RAILPACK"
buildCommand = "pnpm turbo build --filter=@agora/api"
watchPatterns = [
  "/apps/api/**",
  "/packages/common/**",
  "/packages/db/**",
  "/packages/ipfs/**",
  "/packages/chain/**",
  "/packages/scorer/**",
  "/scripts/**",
  "/package.json",
  "/pnpm-lock.yaml",
  "/pnpm-workspace.yaml",
  "/turbo.json",
  "/tsconfig.base.json",
]

[deploy]
startCommand = "pnpm --filter @agora/api start"
healthcheckPath = "/healthz"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
